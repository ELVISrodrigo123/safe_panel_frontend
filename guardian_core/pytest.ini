[tool:pytest]
DJANGO_SETTINGS_MODULE = guardian_core.settings
python_files = tests.py test_*.py *_tests.py
addopts = 
    --cov=.
    --cov-report=term-missing
    --cov-fail-under=0
    -v
    --tb=short
testpaths = 
    .
filterwarnings =
    ignore::DeprecationWarning